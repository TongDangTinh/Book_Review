<h2><%= @book.title %></h2>
<h3><%= @book.author %></h3>
<p><%= @book.description %></p>

<%= link_to t(".back"), root_path %>
<%= link_to t(".edit"), edit_book_path(@book) %>
<%= link_to t(".delete"), book_path(@book), method: :delete, 
  data: { confirm: t(".are_you_sure") } %>
